aiohttp==3.9.1  # Specify the version if needed
Flask==3.0.0    # Specify the version if needed
pymongo==4.6.1 # Specify the version if needed
requests==2.31.0 # Specify the version if needed
google-auth==2.25.2 # Specify the version if needed
google-auth-oauthlib==1.2.0 # Specify the version if needed
Flask-GraphQL==2.0.1 
graphene==2.1.9
graphene-sqlalchemy==2.3.0
graphql-core==2.3.2
graphql-relay==2.0.1
graphql-server-core==1.2.0

